<section class="relative h-[400px] flex items-center justify-center overflow-hidden">
    <img src="{{$imageUrl}}" alt="Background" class="absolute inset-0 w-full h-full object-cover">
    <div class="absolute inset-0 bg-black/40"></div>
    <div class="relative text-center text-white">
        <h1 class="text-5xl font-bold mb-2">{{$pageTitle}}</h1>
        <p class="text-lg opacity-90 font-medium italic">{{$pageSubTitle}}</p>
    </div>
</section>